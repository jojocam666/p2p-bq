import cryptowia
from time import time


class Transaction:
  def__init__(self):
    self.input_transaction = []
    self.output_transaction1 = []
    self.output_transaction2 = []
    
def new_transaction(self,sender_public,sender_private,recipient,amount):
  
  
  transaction = {
      output_transaction1 = [
    
      'id' : os.unrandom(64) ,
      
      'sender' : self.sender_publicKey or _get_public_key,      
       
      'amount' : self.amount 
       
      'recipient' : self.recipient
      
      'timestamp': time() or str(datetime.datetime.now()),
      ]
    
      output_transaction2 = [
    
      'id' :  os.unrandom(64) ,
      
      'sender' : self.sender_publicKey or _get_public_key,
      
      'recipient' : self.bpsc_wallet,
      
      'amount' : self.fees_amount,
      
      'timestamp': time() or str(datetime.datetime.now()), 
      
      
      ]
   
      output_transaction3 = [
      
      'id' : os.unrandom(64),
      
      'sender' : self.sender_publicKey or _get_public_key,
      
      'recipient' : self.sender_publicKey or _get_public_key,
      
      'amount' : self.refund_amount,
      
      'timestamp': time() or str(datetime.datetime.now()),
   
      ]
   
      crypted_transaction =
   } 
